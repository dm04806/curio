<div class="iconed-text {{cls}}">
  <div class="icon">
    <span class="icon-{{icon}}"></span>
  </div>
  <div class="text">
    {{#if num includeZero=true}}
      <strong>{{num}}</strong><br>
    {{/if}}
    {{#if signed includeZero=true}}
    <strong>
      {{#compare signed '>' 0}}+{{/compare}}{{signed}}
    </strong><br>
    {{/if}}
    <span>{{t text}}</span>
  </div>
</div>
