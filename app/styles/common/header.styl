#header
  position: relative
  z-index: header_index

.navbar
  margin-bottom: 0
  border-bottom: 1px solid darken(bg_header, 10%)

.navbar-brand
  width: sidebar_width
  background: lighten(bg_header, 4%)
